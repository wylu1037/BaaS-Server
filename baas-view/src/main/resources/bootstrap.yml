# 先与application.yml文件读取
spring:
  application:
    name: baasViewService
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.1.79:8848
      config:
        file-extension: yaml
        server-addr: ${spring.cloud.nacos.server-addr}
        namespace: de4c568f-a50b-414a-9adc-23d1b2572565
        group: view